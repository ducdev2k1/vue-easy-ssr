/**
 * useHead - Re-export from @vueuse/head
 *
 * Sử dụng @vueuse/head để quản lý SEO meta tags.
 * Package này đã tích hợp sẵn SSR support.
 */

export { createHead, renderHeadToString, useHead } from "@vueuse/head";
export type { Head, HeadObject, HeadTag } from "@vueuse/head";
